const Checkout = () => (
  <div className="panel">
    <h2>Checkout</h2>
    <p style={{ color: 'var(--text-muted)', marginBottom: '1rem' }}>
      Secure checkout is ready for integration with the backend order API.
    </p>
    <button className="btn btn-primary">Place Order</button>
  </div>
)

export default Checkout
